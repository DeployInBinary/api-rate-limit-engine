[project]
name = "api-rate-limit-engine"
version = "0.1.0"
description = "A sliding-window API rate limiting engine"
authors = [{ name="DeployInBinary" }]
requires-python = ">=3.8"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
